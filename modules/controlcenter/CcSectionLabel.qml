import QtQuick
import "../../common"

// A small section heading inside a detail page ("Output", "Input", "Devices").
StyledText {
    leftPadding: Theme.space2
    role: "caption"
    color: Theme.secondaryLabel
}
